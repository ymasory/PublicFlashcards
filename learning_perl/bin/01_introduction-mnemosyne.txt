Guarantee your script will be run with Perl 5.10.	<pre>use 5.010;</pre>
Newer Perl versions allow what in place of <tt>print</tt>?<br><br>How do you activate it?	<tt>say</tt> (adds a <tt>\n</tt>)<br><br><pre>use feature qw(say);</pre>
Is whitespace significant in Perl?	No.
Statements end with ...	... a semicolon.
Run an external program and capture its output.	Write the program name surrounded with back ticks.
