What's the difference between an argument and an operand?	An argument is given after the instruction mnemonic, i.e. present in the program text.<br>Operands are values at the top of the stack, known only at run-time.
