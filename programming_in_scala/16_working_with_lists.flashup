#GRAMMAR 1
#TOPLEFT Programming in Scala
#TOPRIGHT 16

* How are `Lists` implemented?
As linked lists.

* What is the consequence of `Lists` being covariant?
For types S,T if S is a subtype of T, then
`List[S]` is a subtype of `List[T]`.

 
